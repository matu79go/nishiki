# MODELS.yaml — candidate model catalog for the target (Nishiki domain, canonical).
#
# Auto-generated by nishiki init (src=derived from target code / bedrock=actual list_foundation_models enumeration).
# origin: src=wired-in current model (always kept as the baseline) / bedrock=catalog enumeration / openrouter=cross-border (needs residency approval).
# price = USD / 1M tokens (in, out). price_src: A=vendor-published / B=AWS price list (verify ap-ne).
# call: on_demand=direct modelId / profile=via inference profile (jp./apac.=data residency).
# quality_hint: known rating for Japanese company-name/bank-name OCR (good/trap/unknown=unrated=confirm via probe).

models:
  gpt-5-nano:        { origin: openrouter, model_id: openai/gpt-5-nano, call: openai_compatible, in: 0.05, out: 0.4, price_src: OR, tier: external, quality_hint: unknown, note: "cheap text" }
  llama-4-scout:     { origin: openrouter, model_id: meta-llama/llama-4-scout, call: openai_compatible, in: 0.1, out: 0.3, price_src: OR, tier: external, quality_hint: unknown, note: "open" }
  qwen3-vl:          { origin: openrouter, model_id: qwen/qwen3-vl-235b-a22b-instruct, call: openai_compatible, in: 0.2, out: 0.88, price_src: OR, tier: external, quality_hint: unknown, note: "production-floor equivalent = baseline rank" }

# Default run sets: ALL=all 3 / KNOWN=the 0 with origin:src / NEW=the 0 with origin:bedrock.
